-# .header
-#   .header__logo
-#   グループを作成しました  
.wrapper

  = render 'devise/shared/side_bar'  
-# .side-bar
-#   .side-bar__header
-#     .side-bar__header__box
-#       .side-bar__header__box__user-name
-#         = current_user.name
-#         -# test-user
-#       .side-bar__header__box__menu
-#         .side-bar__header__box__menu__new-group
-#         = link_to new_group_path do
-#           = fa_icon 'pencil-square-o', class: 'icon'
-#         -# %label.icon{for: "icon"}
-#         -#   %i.fa.fa-edit.fa-lg
-#         .side-bar__header__box__menu__edit-user
-#         = link_to edit_user_password_path(current_user) do
-#           = fa_icon 'cog', class: 'icon'
-#         -# %label.icon{for: "icon"}
-#         -#   %i.fa.fa-cog.fa-lg
-#   .groups
-#     .groups__messages
-#       .group
-#         -# - current_user.groups.each do |group|
-#         -# = link_to '#' do
-#         .group__group-name
-#           -# = group.name
-#         .group__latest-message
-#           -# メッセージはまだありません。
-#       .group
-#         .group__group-name
-#           sample-group
-#         .group__latest-message
-#           hello!
-#       .group
-#         .group__group-name
-#           example
-#         .group__latest-message
-#           まだメッセージがありません
.chat-main
  .main-header
    .main-header__left-box
      .main-header__left-box__current-group
        test-group
      .main-header__left-box__member-list
        Menber:user
      .main-header__left-box__member-list__menber

    .main-header__edit-btn
      Edit
  .messages
  
    .message
      - @messages.each do |message|
        .message__upper-info
          .message__upper-info__talker
            = message.user.name
          .message__upper-info__date
            = message.created_at.strftime("%Y/%m/%d %H:%M")
        .message__text
          - if message.content.present?
            %p.lower-message__content
              = message.content
          = image_tag message.image.url, class: 'lower-message__image' if message.image.present?
        
    
    .form
      = form_for [@group, @message] do |f|
        = f.text_field :content, class: 'form__message', placeholder: 'type a message'
        .form__mask
          = f.label :image, class: 'form__mask__image' do
            = fa_icon 'picture-o', class: 'icon'
            = f.file_field :image, class: 'hidden'
        = f.submit 'Send', class: 'form__submit'
  